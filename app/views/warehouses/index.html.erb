<div class="container">
  <div class="d-flex align-items-center justify-content-between">
    <h1>Warehouses</h1>
    <%= link_to 'New', new_warehouse_path, class: 'btn btn-primary' %>
  </div>
  <div class="row">
    <div class="table-responsive">
      <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Code</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% @warehouses.each do |warehouse| %>
            <%= render partial: 'warehouses/index/table_row', locals: { warehouse: warehouse }%>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>